import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Ionic Market Google Update',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
